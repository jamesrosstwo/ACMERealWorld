instruction: "Push the green T shape into the black outline of the T shape on the table"

# Degrees of freedom to freeze (lock to home pose / zero in observations).
# For planar pushing we constrain the robot to the table surface with a
# fixed wrist orientation and no gripper actuation.
freeze_z: true
freeze_rotation: true
freeze_gripper: true
